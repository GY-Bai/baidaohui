{
  "name": "baidaohui-web",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "pnpm --parallel dev",
    "build": "pnpm --recursive build",
    "preview": "pnpm --parallel preview",
    "check": "pnpm --recursive check",
    "check:watch": "pnpm --recursive check:watch",
    "lint": "pnpm --recursive lint",
    "format": "pnpm --recursive format"
  },
  "dependencies": {
    "qrcode": "^1.5.3"
  },
  "devDependencies": {
    "@tailwindcss/typography": "^0.5.10",
    "@types/qrcode": "^1.5.5",
    "autoprefixer": "^10.4.16",
    "postcss": "^8.4.32",
    "tailwindcss": "^3.3.6"
  }
} 